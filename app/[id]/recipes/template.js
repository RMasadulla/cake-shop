export default function RecipeTemplate({ children }) {
  return (
    <div className="bg-white text-gray-900">
      <main className="container mx-auto px-4 py-8 mt-[100px]">{children}</main>

      <h1>ok done</h1>
    </div>
  );
}
